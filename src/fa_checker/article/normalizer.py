"""Article text normalization placeholder."""


def normalize_article_text(text: str) -> str:
    """Return lightly normalized text until full normalization rules are implemented."""
    return " ".join(text.split())

